# Service terminal editable sources

| Source | Role |
|---|---|
| `service-terminal-v1.blend` | Blender-authored reversible fallback; canonical comparator |
| `service-terminal-v1__tripo-candidate-01-v2.blend` | Retained material-revision input with lifted housing and exact violet screen material |
| `service-terminal-v1__tripo-candidate-01-v3.blend` | Orientation-normalized review source with the display on exported `-Z` |

The generated candidate does not replace or modify the authored fallback.
Transient v1 and rejected-attempt binaries are reproducible through the
versioned scripts but are intentionally omitted from this review branch.
Their exact hashes, operations, metrics, and named failures remain in the run
metadata and `derived/v3/orientation-fix-report.json`. The review-axis
correction is documented in `derived/v3/publication.md`. The generated
candidate remains rejected at the bake-off geometry hard gate.
